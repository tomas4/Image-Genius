# Default normalize text → LF in repo, native on checkout
* text=auto

# Ensure scripts use LF (important on Linux)
*.sh   text eol=lf
*.ts   text eol=lf
*.js   text eol=lf
*.css  text eol=lf
*.html text eol=lf

# Binary files: never touch line endings
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.pdf  binary
